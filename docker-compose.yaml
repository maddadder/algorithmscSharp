version: '2'

services:

  calculator:
      image: neon-registry.d95f-98d9-33df-f8a6.neoncluster.io/leenet/calculator:1.11.78
      build:
          context: ./
          dockerfile: ./Dockerfile
      restart: unless-stopped
      ports:
          - "5001:443"
